# Sheriff configuration. All available keys and their defaults are in .sheriff/config.yaml.
override:
    ci.pr.max_lines_changed: 700
    phpmetrics.coupling.max_afferent: 30
append:
    phpstan.parameters:
        haspadar:
            noActorSuffix:
                allowedWords:
                    - Container
                    - Docker
            afferentCoupling:
                excludedClasses:
                    - 'Goblin\GoblinException'
